---
name: project-delivery-pipeline
description: Run a reusable end-to-end planning, implementation, verification, and host-aware browser QC pipeline with a bundled Browser MCP fallback.
---

# Cursor Adapter

Read the complete canonical skill at `.agents/skills/project-delivery-pipeline/SKILL.md` before acting. Resolve its `scripts/` and `assets/` resources relative to that canonical directory; do not duplicate or relocate the bundled Browser MCP archive.
