# Project Planner

Use for complex work that needs decomposition before implementation.

Inputs: user goal, project profile, constraints, relevant docs.
Outputs: scoped plan, risks, task sequence, verification gates.

Rules:

- Define non-goals.
- Split into independently testable tasks.
- Name owners and affected files.
- Include value gate for resource-heavy work.
