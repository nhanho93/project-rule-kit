# RULEKIT SKILL DEPTH HARDENING — REMAINING WAVES

CURRENT EXECUTION POINTER: `W5-ZERO-WARNING-CLOSE`

# TASK CLASSIFICATION

TASK_CLASSIFICATION: complex
SIGNALS: multi_file,shared_contract,architecture_change

# GOAL

Commit the verified Wave 1 candidate, deepen every remaining generic scaffold,
close every remaining skill-quality warning through capability-specific
contracts, and finish with 54 registered skills, zero generic scaffolds and zero
quality warnings. Preserve trigger ownership, safety classification, progressive
disclosure and cross-platform portability.

# NON GOALS

- Do not import an external catalog, provenance text or project-specific rules.
- Do not merge distinct skills solely to reduce counts.
- Do not add generic headings or examples that fail to change agent behavior.
- Do not change runtime applications, production, databases or browser surfaces.
- Do not push, publish, release or rewrite history; local wave commits only.
- Do not describe Antigravity or another agent as inherently weak at coding.

# DISCOVERY EVIDENCE

- Wave 1 is phase-closed and uncommitted with 54 skills, 26 generic scaffolds
  and 120 warning codes.
- Wave 2 owns eight core-engineering generic skills and 24 warnings:
  database design, Node.js, Next.js/React, frontend design, PowerShell, server
  management, performance profiling and webapp testing.
- Wave 3 owns eight safety/architecture generic skills and 23 warnings:
  anti-monolith, app builder, MCP builder, vulnerability scanning, red-team,
  localization, Tailwind and web-design guidelines.
- Wave 4 owns ten specialist generic skills and 29 warnings: Bash, behavioral
  modes, documentation templates, game development, GEO, mobile design, Python,
  Rust, SEO and advanced UI/UX.
- Wave 5 owns 44 structural warnings across 20 non-generic skills: missing
  when-to-use boundaries, limitations or worked examples.
- The catalog validator freezes warning and risk metadata. The semantic-drift,
  desired-state, selection, registry and link validators bind derived artifacts.

# IMPLEMENTATION STEPS

1. Deliver the already verified Wave 1 as an exact-path local commit and verify
   its SHA/content before opening Wave 2.
2. For each remaining wave, lock a per-skill contract: owned trigger,
   anti-trigger, discovery/root-cause work, execution steps, limitations, stop
   conditions, capability-specific example and completion evidence.
3. Implement only that wave's canonical skills. Add progressive references only
   when conditional detail would otherwise make an entrypoint exceed 200 lines.
4. Run focused depth checks, catalog/registry/link gates and a Standards x Spec
   review before removing exactly that wave's warning codes.
5. Refresh semantic drift and desired-state artifacts only after the reviewed
   delta matches the wave inventory. Run the full fixture/doctor matrix.
6. Reconcile all three continuity records, evidence and current pointer, then
   commit the verified wave locally before opening the next edge.

# DELIVERY SLICES

SLICE W1-DELIVER: Commit the verified eight-skill agent-control candidate as one local wave.
SLICE W2-CORE-ENGINEERING: Deepen eight core engineering skills and remove exactly 24 warnings.
SLICE W3-SAFETY-ARCHITECTURE: Deepen eight safety and architecture skills and remove exactly 23 warnings.
SLICE W4-SPECIALISTS: Deepen ten specialist skills and remove exactly 29 warnings.
SLICE W5-ZERO-WARNING-CLOSE: Resolve 44 structural warnings across twenty mature skills and close at zero.

Wave completion criteria:

- `W1-DELIVER`: exact Wave 1 files committed; post-commit tree contains only
  this plan/receipt work; SHA and no-push status recorded.
- `W2-CORE-ENGINEERING`: warnings `120 -> 96`, generic `26 -> 18`.
- `W3-SAFETY-ARCHITECTURE`: warnings `96 -> 73`, generic `18 -> 10`.
- `W4-SPECIALISTS`: warnings `73 -> 44`, generic `10 -> 0`.
- `W5-ZERO-WARNING-CLOSE`: warnings `44 -> 0`, generic remains `0`, all skill
  behavior/depth validators and the complete doctor matrix PASS.

For every slice, real-browser E2E is not required because only portable skill,
validator and documentation contracts change. CLI failure injection and
selection/depth validation are the observable acceptance surfaces.

# DEPENDENCY EDGES

W1-DELIVER <- NONE
W2-CORE-ENGINEERING <- W1-DELIVER
W3-SAFETY-ARCHITECTURE <- W2-CORE-ENGINEERING
W4-SPECIALISTS <- W3-SAFETY-ARCHITECTURE
W5-ZERO-WARNING-CLOSE <- W4-SPECIALISTS

# ACCEPTANCE CRITERIA

- All 54 skill entrypoints have distinct selection metadata, explicit
  limitations and capability-appropriate examples; routers may use a routing
  example rather than executable code.
- Every required step ends in observable evidence or a stop condition.
- Generic boilerplate phrase count and quality warning count are both zero.
- No trigger collision, risk downgrade, semantic drift, broken link, desired
  state mismatch, project/source trace or credential-like value is introduced.
- Security/offensive skills retain authorization and safe-boundary contracts.
- Testing, browser QC, planning, Git, VM and deployment skills keep distinct
  ownership and do not substitute for each other's evidence.
- Each warning-baseline diff removes only the declared wave entries.
- Every wave updates handover, action list, pending work and a raw evidence file
  before its local commit. Push remains pending operator authorization.

# VERIFICATION

For each wave:

1. Node syntax for all root/template scripts.
2. Focused skill-depth validator and negative fixtures for the wave.
3. `node scripts/check-skill-catalog.mjs` with exact expected counts.
4. Registry, link, selection, semantic-drift and desired-state validators.
5. Installer, stack, link, registry, selection, drift, knowledge-loop,
   compliance and doctor failure-injection matrix.
6. Standards x Spec diff review, duplicate/trigger search, source/credential
   trace scan and `git diff --check`.
7. Exact staged path list, staged diff scan and post-commit SHA verification.

# QC DECISION

E2E_DECISION: NOT_REQUIRED. The repository has no changed web route, UI,
browser-consumed API, runtime database or authentication surface. CLI behavior
fixtures, selection evidence, warning regression, semantic drift and negative
depth checks are mandatory and cannot be skipped.

# ROLLBACK

- Before a commit, preserve the prior baseline and revert only current-wave
  patches if any exact count or behavior gate fails.
- After a local commit, recover with an additive revert commit; do not reset or
  rewrite history.
- If a wave cannot remove its exact warning set without trigger overlap or
  duplicated rules, keep the warnings frozen, record the owner/action and block
  later waves.

# APPROVAL

APPROVAL_STATUS: APPROVED

The operator explicitly requested completion of all suggested waves. This
authorizes bounded local implementation and local commits needed to close the
waves. It does not authorize push, publish, release, deployment or history
rewrite.
